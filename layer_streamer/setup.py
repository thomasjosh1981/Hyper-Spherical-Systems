from setuptools import setup, find_packages

setup(
    name="layer-streamer",
    version="1.0.0",
    description="CPU & GPU Hyperspherical Layer Streamer for Oversized GGUF/Safetensors Models",
    packages=find_packages(),
    python_requires=">=3.8"
)
